import * as React from 'react';
import { Text, View, StyleSheet, Image } from 'react-native';

export default class BookcaseItem extends React.Component {

    render() {
      return (
        <Text>BookcaseItem</Text>
      );
    }
  }